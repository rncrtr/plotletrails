<%= form_for(@col) do |f| %>
  <% if @col.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@col.errors.count, "error") %> prohibited this col from being saved:</h2>

      <ul>
      <% @col.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :plot_id, {:value => params[:plot_id]} %>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :ord %><br />
    <%= f.number_field :ord %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
